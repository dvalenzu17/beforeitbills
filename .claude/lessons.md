| Date | What went wrong | Rule to prevent it |
|------|----------------|-------------------|
| 2026-03-30 | Claude over-flagged one-time charges as recurring | Check for >= 2 occurrences with consistent interval before flagging |
| 2026-04-01 | axError | After editing JS files that contain single-quoted strings, verify no non-ASCII quote chars were introduced (codes 8216/8217) |
| 2026-04-17 | Hermes VM segfault (arrayPrototypeSome) on iOS launch | Array methods called on null/undefined during initial render before async data resolves — always initialise state as [] not null for arrays; guard all .some()/.filter()/.map()/.reduce() calls on store state or async data with ?.method() or (val \|\| []).method() |
| 2026-06-09 | App Store rejection 3.1.2: hardcoded "$4.99/month" in onboarding CTA & locale files didn't match actual RevenueCat product ($7.99/mo or $34.99/yr). Missing Restore Purchases on onboarding paywall. Missing auto-renewal/cancellation terms. | NEVER hardcode prices — always use `product.priceString` from RevenueCat. Every screen with a purchase CTA MUST have: (1) Restore Purchases button, (2) auto-renewal disclosure, (3) cancellation instructions referencing App Store Settings. |
| 2026-06-09 | App Store rejection 2.1.0: reviewer couldn't complete a core flow because no demo account was provided in App Store Connect review notes. Google OAuth won't work in review sandbox. | Before every App Store submission: create a demo account in Supabase with `onboarding_done: true` + sample data, enter credentials in App Store Connect review info, include review notes explaining email+password sign-in path. See APP_STORE_REVIEW.md. |
| 2026-06-09 | App Store rejection 4.0.0: sign-in screen had no app branding/context — reviewer saw a generic login form with no explanation of what the app does. "Not now" label on paywall was ambiguous about free tier. | Sign-in screen must show app logo + tagline. Any paywall "skip/dismiss" button must clearly communicate free tier availability (e.g. "Continue with free features" not "Not now"). |
| 2026-06-09 | App Store rejection 2.1.0 (continued): onboarding `goNotNow()` and `goGetStarted()` relied solely on Supabase `USER_UPDATED` auth event to trigger navigation from onboarding → main app. If Supabase was slow or the event didn't fire in time, the user appeared stuck after tapping "Continue with free features". | After calling `setOnboardingDone(true)`, ALWAYS follow with an explicit `router.replace("/(tabs)")`. Never rely solely on indirect auth-listener-driven navigation for user-initiated transitions — the user must see immediate feedback. |